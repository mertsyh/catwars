# OpenFrontClone — Proje Durumu

OpenFront.io / territorial.io tarzı, tarayıcı tabanlı, gerçek zamanlı çok oyunculu bir toprak fetih oyununun klonu — küçük ölçekli (1-2 insan oyuncu) ama artık bot sayısı başlangıç ekranından 0-400 arası ayarlanabiliyor (varsayılan 20). Bu dosya, o ana kadar neyin yapıldığını ve bundan sonra nereye gidilmek istendiğini özetler — yeni bir konuşmada kaldığımız yerden devam edebilmek için.

## Teknoloji Yığını

- **TypeScript** (tüm proje), **Vite** (client dev server/build), **Node.js + `ws`** (server), **tsx** (server'ı TS olarak doğrudan çalıştırmak için)
- Paket yöneticisi: npm. `npm run dev` hem client (5173) hem server'ı (3000) `concurrently` ile başlatır.
- Node.js kurulu ama sistem PATH'ine eklenmemişti; kullanıcı `PATH`'ine kalıcı olarak eklendi (`C:\Program Files\nodejs`).

## Mimari

```
src/
  core/     → paylaşılan, deterministik oyun mantığı (client + server ikisi de import eder)
  client/   → Vite giriş noktası, Canvas2D render, input/UI (Node'suz, tarayıcıda çalışır)
  server/   → WebSocket sunucusu, tick döngüsü, bot AI
```

`core` katmanı render veya network'ten tamamen bağımsızdır — bu sayede hem server (yetkili simülasyon) hem de gelecekte client-side prediction aynı kodu paylaşabilir.

### `core` dosyaları
- `GameMap.ts` — terrain (kara/deniz) ızgarası, `owner` (tile→oyuncu) dizisi. `fromTerrain()` (Faz 8) önceden üretilmiş bir terrain grid'inden kurar — sunucu bunu `resources/maps/<id>.json` ile kullanıyor (Faz 9: hangi `id` seçildiğine göre). `generateIsland()` placeholder/"Rastgele Ada" dairesel üretimi, dosya yoksa fallback olarak da korunuyor.
- `maps.ts` (Faz 9) — `MAP_REGISTRY` (istemcinin harita seçim dropdown'ında ve sunucunun `mapId` doğrulamasında kullandığı ortak liste), `WORLD_MAP_SPECS` (build-time bbox'lar), `DEFAULT_MAP_ID`, `RANDOM_ISLAND_MAP_ID`.
- `Player.ts` — oyuncu kaynakları (asker, altın, toprak sayısı, şehir sayısı), `maxTroops` hesaplaması, toprağın centroid'i (`centerX`/`centerY`, O(1) koşan toplamla güncellenir — haritadaki asker etiketinin konumu için).
- `GameState.ts` — **oyunun kalbi**: oyuncu ekleme/çıkarma, açık haritada rastgele doğuş (`spawnHome`), tile-tabanlı kuşatma (siege) sistemi, bina inşası, tick döngüsü, kazanma koşulu, bot AI hedef seçimi için oyuncu başına "sınır" (frontier) tile seti.
- `protocol.ts` — client↔server WebSocket mesaj tipleri.
- `constants.ts` — tüm denge sabitleri (aşağıda).

> **Not (2026-07-30):** Önceden burada `regions.ts` (Voronoi tabanlı, ~80 isimli "ülke" bölgesi) ve `civilizationNames.ts` (bölge isim havuzu) vardı — bkz. aşağıdaki "Bölge sisteminin kaldırılması: açık harita" bölümü. Bu iki dosya artık yok; bölge kavramı koddan tamamen çıkarıldı.

### `server/index.ts`
- İlk oyuncu katıldığında haritayı yükler, `GameState` oluşturur, `join` mesajındaki `botCount`'a göre (0..`MAX_BOT_COUNT`) bot spawn eder.
- Her 800ms'de bot'lara kendi sınır (frontier) tile'larından rastgele bir hedefe saldırı yaptırır (`getBotAttackTarget`).
- WebSocket bağlantılarını `Player`'a eşler, `join`/`attack`/`build`/`cancelAttacks` mesajlarını işler.
- 10 tick/saniye ana döngü: `GameState.tickOnce()` çağırır, değişiklikleri tüm client'lara yayınlar.

### `client/` 
- `renderer.ts` — Canvas2D üzerinde sürekli `requestAnimationFrame` döngüsü ile çizim: tile renklendirme (kuşatma ilerledikçe gerçek tile renkleri anlık güncellenir, ayrı bir "kuşatma ilerleme" overlay'i yok), sahiplik sınırları (koyu ton, sahiplik değiştikçe tek tile bazında güncellenir — bkz. `setBorderFlag`), oyuncu/bot asker sayısı etiketleri (`setPlayerLabels`), bina ikonları, hover vurgusu, tıklama ripple animasyonu, kamera (pan/zoom).
- `main.ts` — WebSocket bağlantısı, input yönetimi (sürükle=pan, tekerlek=zoom, tık=saldır/inşa, sağ tık=iptal), UI paneli, bina barı.

## Şu An Çalışan Oyun Mekaniği

1. **Harita**: gerçek dünya kıyı şeridi (veya prosedürel "Rastgele Ada") — bölge/ülke kavramı yok, tamamen açık, sahiplenilmemiş kara. Sınırlar (koyu kenarlar) artık önceden çizilmiyor, sadece fiilen kimin toprağı kime bitişikse orada oluşuyor (bkz. "Bölge sisteminin kaldırılması" altında).
2. **Doğuş**: Her oyuncu (insan veya bot) haritada rastgele, mümkün olduğunca boş bir noktada **küçük yuvarlak bir toprak parçasıyla** (~20 tile, `SPAWN_TILE_COUNT`) doğar — "herkes küçücük başlar, sonra büyür" prensibi. Bot'lar 1000, insan oyuncular 2500 askerle başlar (`BOT_STARTING_TROOPS`/`PLAYER_STARTING_TROOPS`).
3. **Kaynaklar**: Asker (troops, saldırı/savunma için harcanır) ve Altın (pasif, toprağa bağlı birikir, henüz sadece bina inşasında kullanılıyor).
4. **Saldırı = Süregelen Cephe**: Kendi toprağına bitişik bir tile'a tıklayınca, o tile'ın o anki sahibine (nötr=-1 ya da belirli bir düşman oyuncu) karşı **süregelen bir cephe** açılır — tek seferlik sabit boyutlu bir alan değil. Cephe her tick otomatik ilerler: saldırganın sınırının hedefe dokunduğu HER noktadan, harcanan askerle orantılı sayıda tile **anında saldırgana geçer**, tıklanan noktaya en yakın tile'lar önce alınır (o yöne doğru büyüme hissi). Bu, hedefin o sınırdaki toprağı tamamen tükenene ya da iptal edilene kadar **tek bir tıklamayla kendiliğinden sürer** — pikselleri tek tek tıklamak gerekmez (bkz. `GameState.ts`'teki `edgeTilesFor`/`tickOnce`).
   - Aynı hedefe (nötr veya belirli bir düşmana) karşı tekrar tıklamak mevcut cepheyi **boost**'lar (hız çarpanı artar, max 4x) ve odak noktasını günceller.
   - Bir düşmanın sınırına ulaştığında, o düşmana doğru ilerlemeye devam etmek için **onun toprağına tıklamak** gerekir — nötr toprağa açılan cephe otomatik olarak bir sonraki oyuncuya sıçramaz, her hedef kendi tıklamasını ister.
   - Sağ tık, o oyuncunun **tüm aktif cephelerini iptal** eder (o ana kadar alınmış tile'lar sende kalır, sadece geri kalanından vazgeçmiş olursun).
   - Düşman toprağını kuşatmak nötr topraktan **1.5x** daha pahalıdır.
   - Sadece kendi toprağına 4 yönlü bitişik bir tile'a saldırı BAŞLATABİLİRSİN (cephe açıldıktan sonra kendiliğinden ilerler).
   - **Karşılıklı savaş vurgusu**: iki oyuncu aynı anda birbirine karşı cephe açmışsa (A→B ve B→A), aradaki cephe hattı tile'ları haritada **kırmızı** vurgulanır — "o beni itiyor, ben onu itiyorum" hissi (bkz. `GameState.computeContestedTiles`, `renderer.ts`'teki `drawContestedTiles`). Bu yüzden oyuncu renk paletinden (`PLAYER_COLORS`) kırmızı bilinçli olarak çıkarıldı, karışmasın diye.
5. **Binalar**: Ekranın altındaki bar'dan Şehir (300 altın, +300 asker kapasitesi + sürekli ek asker üretimi) veya Savunma Karakolu (200 altın, etrafında **4 tile yarıçaplı, yuvarlak, haritada görünen bir etki alanı** — bu alandaki her fetih daha maliyetli/yavaş olur ve saldırgan bir tile alırken anlık ek asker kaybeder) seçilir, sonra haritada kendi tile'ınıza tıklanır. Bir tile ele geçirilirse üzerindeki bina yıkılır.
6. **Bot AI**: Bot sayısı başlangıç ekranından seçilir (varsayılan 20, en fazla 400 — `MAX_BOT_COUNT`). Her bot periyodik olarak kendi sınır (frontier) tile'larından rastgele birini seçip bitişik bir sahipsiz/düşman tile'a kuşatma başlatır — insan oyuncuyla aynı `queueAttack` API'sini kullanır. Sınır tile seti her sahiplik değişikliğinde O(1) güncellenir, böylece 400 bot'ta bile haritayı taramaz.
7. **Kazanma koşulu**: Toprağın %72'sini kontrol eden oyuncu kazanır.
8. **Asker etiketleri**: Her oyuncu/bot'un toprağının centroid'i üzerinde, kendi rengiyle güncel asker sayısı (ör. "2.5k") yüzen bir etiket olarak gösterilir — haritaya yeterince yakınlaşınca (`LABEL_MIN_SCALE`) görünür.
9. **Kamera/kontroller**: Doğuşta oyuncunun toprağına otomatik zoom, fare tekerleğiyle zoom (imlece göre), sürükleyerek pan, hover'da geçerli/geçersiz hedef rengi (yeşil/kırmızı/beyaz), her tıklamada anlık ripple animasyonu (görsel geri bildirim).
10. **Liman + Ticaret Gemisi** (Faz 6): kıyı bir tile'a Liman (450 altın, her ek liman 2x pahalılaşır, tavan 3600) inşa edilebilir. Limanı olan oyuncular arasında periyodik olarak otomatik **ticaret gemileri** doğar, su üzerinde BFS ile bulunmuş rotayı takip ederek karşı limana ulaşır ve varışta her iki liman sahibine altın öder. Bot'lar da düşük olasılıkla liman inşa eder.
11. **Savaş Gemisi + Deniz Savaşı** (Faz 7): limanda Savaş Gemisi (800 altın, her ek gemi +400, tavan 3000; 3 sn inşa süresi) inşa edilip suyun herhangi bir noktasına yönlendirilebilir (gemiye tıkla=seç, suya tıkla=hareket ettir, sağ tık=seçimi iptal et). Menzile giren düşman gemileri otomatik hasarlaşır; HP %30'un altına düşen gemi en yakın kendi limanına kaçıp onarılır, HP 0'a inen gemi yok olur. Düşman savaş gemisi, geçiş yolundaki (ilişkisiz) ticaret gemilerini yakalayıp yok edebilir ve bonus altın kazanır.
12. **Gerçek Dünya Haritası** (Faz 8): placeholder dairesel ada yerine gerçek bir kıyı şeridi — Natural Earth'ün public domain `ne_50m_land` kara poligonu verisinden (`resources/geo-source/`, gitignore'da) Avrupa bbox'ı (`lon -25..45, lat 34..72`) 400×300 tile'a scanline (even-odd) yöntemiyle rasterize ediliyor. `scripts/build-map.ts` (`npm run build:maps`) çıktıyı `resources/maps/europe.json`'a yazıyor; sunucu açılışta bunu okuyor, dosya yoksa placeholder daireye düşüyor.
13. **Çoklu Harita Desteği** (Faz 9): `src/core/maps.ts`'te `MAP_REGISTRY` (Avrupa, Afrika, Kuzey Amerika, Dünya + prosedürel "Rastgele Ada") ve build-time `WORLD_MAP_SPECS` (bbox'lar) tek kaynaktan tanımlanıyor; `scripts/build-map.ts` artık bu listedeki her bölge için ayrı `resources/maps/<id>.json` üretiyor. İstemci artık bağlanmadan önce bir harita seçim ekranı gösteriyor (`index.html`'deki `#startScreen`), seçilen `mapId` `join` mesajıyla sunucuya gidiyor. Sunucu, harita + `GameState`'i (ve bot'ları) artık açılışta değil **ilk oyuncu katıldığında** kuruyor — böylece o oyuncunun seçtiği harita geçerli oluyor; oyun bir kez kurulduktan sonra gelen farklı `mapId`'ler yok sayılıp mevcut harita kullanılıyor (tarayıcıda iki sekmeyle doğrulandı).

## Denge Sabitleri (`src/core/constants.ts`)

| Sabit | Değer | Anlamı |
|---|---|---|
| `TICK_RATE` | 10 | saniyede tick sayısı |
| `PLAYER_STARTING_TROOPS` / `BOT_STARTING_TROOPS` | 2500 / 1000 | doğuş asker sayısı — insan oyuncu bot'tan daha büyük başlar |
| `BASE_MAX_TROOPS` / `TROOPS_PER_TILE` | 2500 / 0.5 | asker kapasitesi = 2500 + toprak×0.5 — taban, `PLAYER_STARTING_TROOPS`'a eşit tutuluyor ki küçük doğuş toprağıyla (bkz. `SPAWN_TILE_COUNT`) doğuş askeri ilk tick'te rejenerasyon sınırına takılıp silinmesin |
| `TROOP_REGEN_PER_TICK` | 0.5 | asker rejenerasyonu |
| `SPAWN_TILE_COUNT` | 20 | doğuşta sahip olunan küçük yuvarlak toprağın hedef tile sayısı |
| `CAPTURE_TILE_COST` | 4 | bir tile'ı ele geçirmenin taban asker maliyeti (üzerinde karakol varsa `DEFENSE_POST_GARRISON_BONUS` eklenir) — cephe süregelen olduğu için artık sabit bir alan/garrison havuzu yok, doğrudan tile başına maliyet |
| `SIEGE_DAMAGE_PER_TICK` | 4 | temel kuşatma hasarı (boost ile çarpılır) — `CAPTURE_TILE_COST`'a eşit tutuldu ki boost'suz bir cephe ~1 tile/tick alsın |
| `ENEMY_SIEGE_COST_MULTIPLIER` | 1.5 | düşman toprağı kuşatma maliyet çarpanı |
| `MAX_BOOST` | 4 | maksimum hızlandırma çarpanı |
| `MAX_BOT_COUNT` / `DEFAULT_BOT_COUNT` | 400 / 20 | başlangıç ekranından seçilebilen bot sayısı sınırı / varsayılanı |
| `BOT_REST_TROOP_FRACTION` / `_ATTACK_...` | 0.15 / 0.4 | bot bu oranın altında TÜM cephelerini iptal edip dinlenir / yeni cephe açmak için asker bu oranın üstünde olmalı |
| `BOT_MAX_CONCURRENT_ORDERS` | 3 | bot'un aynı anda açık tutabileceği en fazla farklı hedef |
| `WIN_LAND_FRACTION` | 0.72 | kazanma eşiği |
| `CITY_COST` / `CITY_TROOP_BONUS` / `CITY_TROOP_REGEN_BONUS` | 300 / +300 / +0.3 | şehir maliyeti / asker kapasitesi bonusu / şehir başına ek asker üretim hızı (tick başına) |
| `DEFENSE_POST_COST` / `_RADIUS` / `..._GARRISON_BONUS` / `..._TROOP_DRAIN` | 200 / 4 tile / +20 / 15 | karakol maliyeti / etki yarıçapı / o yarıçaptaki fetihlere ek maliyet (üst üste biner) / bir tile o yarıçapta ele geçirildiğinde saldırgana anlık ek asker kaybı |
| `PORT_COST_BASE` / `_MULTIPLIER` / `_CAP` | 450 / 2x / 3600 | liman maliyeti (her ek liman katlanır) |
| `MAX_TRADE_SHIPS` | 12 | aynı anda var olabilecek ticaret gemisi |
| `TRADE_SHIP_SPAWN_INTERVAL_TICKS` | 40 (4 sn) | liman başına doğuş denemesi aralığı |
| `TRADE_SHIP_SPEED_TILES_PER_TICK` | 1.5 | gemi hızı |
| `TRADE_SHIP_BASE_GOLD` / `_GOLD_PER_TILE` | 15 / 0.4 | varış geliri = 15 + mesafe×0.4 (iki sahibe yarı yarıya) |
| `TRADE_SHIP_CAPTURE_BONUS_GOLD` | 100 | düşman ticaret gemisi yakalanınca bonus |
| `WARSHIP_COST_BASE` / `_INCREMENT` / `_CAP` | 800 / +400 / 3000 | savaş gemisi maliyeti (doğrusal artış) |
| `WARSHIP_BUILD_TICKS` | 30 (3 sn) | inşa süresi |
| `WARSHIP_MAX_HP` / `_DAMAGE_PER_TICK` | 200 / 20 | can puanı / menzildeki düşmana tick başına hasar |
| `WARSHIP_RANGE` / `_CAPTURE_RANGE` | 6 / 3 (tile) | çatışma menzili / ticaret gemisi yakalama menzili |
| `WARSHIP_SPEED_TILES_PER_TICK` | 1.2 | hareket hızı |
| `WARSHIP_RETREAT_HP_FRACTION` | 0.3 | bu oranın altında otomatik geri çekilme |

## Geliştirme Süreci (kronolojik özet)

1. **Faz 0** — Proje iskeleti: TS + Vite + WS server, placeholder dairesel ada, uçtan uca "harita client'a ulaşıyor" doğrulaması.
2. **Faz 2** — İlk oyun döngüsü: tile-flood tabanlı toprak fethi (tıkla → BFS ile komşu tile'ları yut), asker/altın kaynakları, çok oyunculu senkron.
3. **Faz 4** — Basit bot AI (rastgele sınır hedefi seçip saldırma).
4. **Faz 5** — Şehir binası (asker kapasitesi bonusu).
5. **Savunma Karakolu** — ikinci bina türü, önce tile-yarıçapı tabanlı savunma çarpanı olarak eklendi.
6. **UX düzeltmesi** — kullanıcı geri bildirimi: "tıklayınca bir şey olmuyor, görsel geri dönüş yok". Kök neden: harita her zaman tam ekrana sığdırılıyordu, doğum bölgesi görünmüyordu. Çözüm: kamera pan/zoom, doğuşta otomatik odaklanma, hover vurgusu, tıklama ripple animasyonu.
7. **Saldırı mekaniği yeniden tasarımı** — kullanıcı geri bildirimi: "kendi kendine etrafa saldırıyor, sadece isteğime göre saldırmalıyım, iptal edebilmeliyim, hızlandırabilmeliyim". Tile-flood sınırsızdı; her tıklamayı **60 tile'lık sabit kapasiteli, iptal edilebilir, tekrar tıklamayla hızlanan emirlere** dönüştürdük.
8. **Bölge tabanlı büyük revizyon** — kullanıcı isteği: haritanın rastgele sınırlı, isimli "ülke" bölgelerinden oluşması, saldırının piksele değil bölgeye yapılması, herkesin aynı boyda başlaması, binalar için alt bar. Tile-flood tamamen kaldırıldı, yerine Voronoi bölge + garrison/kuşatma sistemi geldi.
9. **Faz 6 — Liman + Ticaret Gemisi** — [docs/phases/faz-06-liman-ticaret-gemisi.md](docs/phases/faz-06-liman-ticaret-gemisi.md) planına göre uygulandı: `RegionMeta`/`GameMap`'e kıyı tile tespiti, `src/core/pathfinding.ts`'te su üzerinde BFS rota bulma, `Port` binası (katlanan maliyet), `TradeShip` varlığı (doğuş/ilerleme/varış, istemci tarafında `spawnTick`+hız'dan lokal interpolasyon — her tick tam pozisyon yayınlanmıyor). Bot AI'ya düşük olasılıklı liman inşası eklendi.
10. **Faz 7 — Savaş Gemisi + Deniz Savaşı** — [docs/phases/faz-07-savas-gemisi-deniz-savasi.md](docs/phases/faz-07-savas-gemisi-deniz-savasi.md) planına göre uygulandı: `pathfinding.ts` genelleştirildi (`findWaterPath` → `findShipRoute`, kara/su herhangi bir kombinasyonu kabul eder; artı paylaşılan `positionAlongPath` — hem sunucu çarpışma/yakalama menzil kontrolü hem istemci render'ı aynı fonksiyonu kullanır). `Warship` varlığı (inşa→boşta→hareket→dönüş durum makinesi), oyuncu kontrollü hedefleme (gemiye tıkla=seç, suya tıkla=hareket ettir), tick-bazlı `resolveNavalCombat` (düşman çiftleri menzildeyken hasarlaşır, düşük HP'de otomatik geri çekilme, HP 0'da yok olma) ve `captureTradeShips` (düşman ticaret gemisini yakalayana bonus altın) eklendi.
11. **Faz 8 — Gerçek Dünya Haritası** — [docs/phases/faz-08-gercek-dunya-haritasi.md](docs/phases/faz-08-gercek-dunya-haritasi.md) planına göre uygulandı: `GameMap.fromTerrain()` (Node/fs'ten bağımsız, saf veri fabrikası — `core` katmanının client'ta da import edilebilir kalması için dosya okuma işi `server/index.ts`'e bırakıldı). `scripts/build-map.ts`, Natural Earth `ne_50m_land` (public domain, `resources/geo-source/`'a indirilir, commit'lenmez) verisinden Avrupa bbox'ını even-odd scanline algoritmasıyla rasterize edip `resources/maps/europe.json`'a yazıyor (`npm run build:maps`). Sunucu açılışta bu dosyayı okuyor; yoksa placeholder daireye düşüyor.
12. **Faz 9 — Çoklu Harita Desteği** (mevcut durum) — [docs/phases/faz-09-coklu-harita.md](docs/phases/faz-09-coklu-harita.md) planına göre uygulandı: `src/core/maps.ts` eklendi (`MAP_REGISTRY`, `WORLD_MAP_SPECS`, `DEFAULT_MAP_ID`, `RANDOM_ISLAND_MAP_ID`); `scripts/build-map.ts` tek sabit `REGIONS` dizisi yerine bu ortak listeyi kullanacak şekilde genelleştirildi. 5 harita: Avrupa (mevcut), Afrika, Kuzey Amerika, Dünya (tüm gezegen, `lon -180..180`) + prosedürel "Rastgele Ada". `protocol.ts`'teki `JoinMessage`'a opsiyonel `mapId` eklendi. `server/index.ts` büyük ölçüde refaktör edildi: harita + `GameState` artık modül yüklenirken değil, `ensureGame()` içinde **ilk `join` mesajında** kuruluyor (bot spawn'ı da buna taşındı); DTO fonksiyonları (`toDTO`, `toBuildingDTOs`, vb.) modül-seviyesi değişken yerine `state` parametresi alacak şekilde değiştirildi (null-safety için), iki `setInterval` (bot AI, tick döngüsü) `instance` null kontrolü ile korunuyor. İstemciye (`index.html` + `main.ts`) bağlanmadan önce gösterilen bir `#startScreen` (harita dropdown + "Oyuna Katıl" butonu) eklendi; WebSocket bağlantısı artık sayfa yüklenince değil, bu butona tıklanınca açılıyor ve seçilen `mapId` `join` mesajına ekleniyor. İki ayrı tarayıcı sekmesiyle uçtan uca doğrulandı: ilk sekme "Afrika" seçip oyunu kurdu, ikinci sekme "Kuzey Amerika" seçmesine rağmen zaten kurulu olan Afrika haritasına katıldı (spesifikasyondaki "oyun kurulduysa mapId yok sayılır" davranışı).
    - **Takip düzeltmesi (aynı faz içinde, kullanıcı geri bildirimiyle):** ilk sürümde Afrika/Kuzey Amerika kıtaları bbox'a çok sıkı oturtulmuştu (kıyı şeridi harita ızgarasının kenarına neredeyse değiyordu) — bu, o bölgeye yakınlaşan bir oyuncunun ekranının bir kısmının harita dışına (siyah arka plana) taşmasına yol açıyordu. Kök neden aslında `renderer.ts`'teki `clampCamera()`'ydı: kamerayı sadece `[0, width]` aralığına kırpıyordu, geçerli zoom'da görünür alanın (canvas/scale) haritadan büyük olup olmadığını hesaba katmıyordu. Düzeltme: `clampCamera()` artık zoom'a göre kamerayı harita kenarından en az yarım-viewport içeride tutuyor (harita görünür alandan küçükse o eksende ortalıyor — bu durum sadece bilinçli "zoom=1'de tüm haritayı göster" letterbox'ı için geçerli). Ayrıca kıta bbox'larına bolca boşluk eklendi (`src/core/maps.ts`) ve kullanıcının istediği **"Dünya"** (tüm gezegen, 570×220) haritası eklendi.
13. **Orantılı parçalı bölge alımı** (mevcut durum) — kullanıcı isteği: "bölgeyi almak için tıkladığımda ne kadar askerim varsa o kadarını alayım, tamamını alana kadar beklemek zorunda olmayalım". Yukarıdaki 8. maddedeki "bölge tabanlı büyük revizyon"un getirdiği **bölge bütünlüğü** (bölge = tek parça, tek sahip) ilkesi bilinçli olarak gevşetildi: bir bölge artık birden fazla oyuncu arasında bölünebilir.
    - `GameState.ts`: `Siege` artık `captureOrder` (saldırganın sınırından içeri BFS sırasıyla, henüz saldırgana ait olmayan tile'lar), `cursor`, kendi `garrison`/`maxGarrison`'ı ve donmuş `costMultiplier`'ı taşıyor — garrison artık bölgede değil, kuşatmada (her kuşatma kendi "fetih projesi"). Her tick, o an harcanan hasarın "satın aldığı" tile sayısı hesaplanıp `captureOrder`'dan o kadarı **anında** saldırgana geçiyor (tile'ın önceki sahibinin `tileCount`/asker/bina kaybı da o anda uygulanıyor) — bölgenin tamamen düşmesini beklemeye gerek yok.
    - `Region.garrison`/`maxGarrison` alanları kaldırıldı (artık `regions.ts`'de yok); `Region.ownerId` artık "tek sahip" değil, tile dağılımına bakılarak hesaplanan **çoğunluk sahibi** (`recomputeRegionOwner`) — sadece kaba bir gösterge.
    - `queueAttack`: "zaten sahibim" kontrolü tam bölge sahipliğine (`regionFullyOwnedBy`), "erişebiliyor muyum" kontrolü ise bölgenin kendisinde veya bir komşusunda en az bir tile'a sahip olmaya (`regionHasAnyTileOwnedBy`) göre değişti — böylece hem yarım bıraktığın bir bölgeyi bitirebiliyorsun hem de sadece bir köşeden (bölgenin tamamı değil) sıçrama yapabiliyorsun.
    - Savunma Karakolu artık bölgeye kalıcı bonus eklemiyor; her yeni kuşatmanın `maxGarrison`'ı, o bölgedeki MEVCUT karakol binalarına bakılarak anlık hesaplanıyor (`regionDefenseBonus`) — bina yıkılırsa bonus da otomatik gider.
    - Client tarafında artık gereksiz olan "yenilen tile" overlay sistemi (`computeEatOrder`/`eatOrderCache`, `renderer.ts`'teki `drawSiegeOverlays`) tamamen kaldırıldı — gerçek tile renkleri zaten anlık güncellendiği için ayrı bir sahte ilerleme efektine gerek kalmadı. Bina inşası / hover geçerliliği kontrolleri bölge çoğunluğu yerine doğrudan tile sahipliğine (`ownerByTile`) bakacak şekilde düzeltildi (aksi halde karışık bir bölgede yanlış tile'a inşaat izni verilebilirdi).
    - Tarayıcıda doğrulandı: küçük bir orduyla komşu bir bölgeye tıklayınca toprak sayısı **anında** (bir sonraki tick'te) artıyor, bot'ların kısmen fethettiği bölgeler haritada ince bir renkli şerit olarak görünüyor, tamamen sahip olunan bölgeye tekrar saldırı ve komşu olmayan bölgeye saldırı doğru şekilde reddediliyor.
14. **Bölge sisteminin kaldırılması: açık harita** (2026-07-30, mevcut durum) — kullanıcı isteği: "haritada daha fazla parça olmalı [...] başlangıçta bölmeler olmayacak herkes küçücük doğacak yuvarlak bir parça olarak, sonra etrafına tıklayarak büyüyecek, botlar 1k asker boyutunda oyuncular 2.5k asker boyutunda olacak, her bir botun/oyuncunun üstünde kaç askeri olduğu yazacak, bot sayısı değiştirilebilir olacak en fazla 400 tane olacak". 8. maddedeki "bölge tabanlı büyük revizyon"un getirdiği isimli Voronoi "ülke" bölgeleri (ve 13. maddedeki üzerine kurulan orantılı parçalı alım) tamamen kaldırıldı — territorial.io tarzı tamamen açık bir haritaya dönüldü, ama **orantılı/iptal edilebilir/boost'lanabilir kuşatma motoru korundu**, sadece hedefi "önceden çizilmiş bölge" değil "tıklanan tile'ın etrafında BFS ile büyüyen sınırlı bir alan" oldu.
    - `src/core/regions.ts` ve `civilizationNames.ts` silindi. `GameMap.regionOf` kaldırıldı, `MapMessage`'dan `regionOf`/`regions` çıktı.
    - `GameState.spawnHome`: yeni oyuncu/bot artık önceden çizilmiş bir bölgeye değil, haritada rastgele denenen birkaç noktadan en boşuna, nötr kara üzerinde BFS ile küçük yuvarlak bir toprağa (`SPAWN_TILE_COUNT`=20 tile) doğuyor.
    - `GameState.queueAttack`/`computeCaptureOrder`: hedef artık `regionId` değil tıklanan `tileIndex` — kendi toprağına 4 yönlü bitişik olmalı. Fetih alanı, o tile'dan başlayıp sadece saldırgana ait OLMAYAN kara tile'ları üzerinden BFS ile en fazla `CAPTURE_CHUNK_MAX_TILES`=60 tile'a kadar büyüyor; `Siege.anchorTile` bu noktayı taşıyor. Aynı fetih alanına düşen bir tekrar tıklama, eskisi gibi mevcut kuşatmayı boost'luyor.
    - **Bot AI için "sınır" (frontier) seti**: `GameState` artık oyuncu başına, en az bir komşusu kendine ait olmayan tile'ların bir dizisini (`frontierTiles`/`frontierIndex`, swap-pop ile O(1) ekleme/çıkarma/rastgele-seçim) tutuyor, her sahiplik değişikliğinde O(1) güncelleniyor. Bot hedef seçimi (`getBotAttackTarget`) ve liman için kıyı tile'ı arama (`getOwnedCoastalTile`) artık haritayı taramak yerine bu seti kullanıyor — 400 bot'a kadar ölçekleniyor.
    - `Player`: `startingTroops` parametreli oldu (bot=1000, insan=2500); `sumX`/`sumY` koşan toplamıyla O(1) `centerX`/`centerY` (centroid) hesaplıyor — haritadaki asker etiketinin konumu için. `BASE_MAX_TROOPS` 100'den 2500'e çıkarıldı (aksi halde küçük doğuş toprağıyla `maxTroops` çok düşük kalıp doğuş askerinin çoğu ilk tick'te silinirdi).
    - `PlayerStateDTO`'ya `centerX`/`centerY` eklendi; `renderer.ts`'e `setPlayerLabels`/`drawPlayerLabels` (bölge isim etiketlerinin yerini alan, oyuncu rengiyle "1.4k" gibi kısaltılmış asker sayısı gösteren yüzen etiket) ve `setBorderFlag` (sınır artık statik `regionOf`'tan değil, sahiplik değiştikçe tek tek tile bazında anlık hesaplanıyor) eklendi.
    - `JoinMessage`'a opsiyonel `botCount` eklendi (sadece oyunun ilk kurulduğu join'de anlamlı, `mapId` ile aynı desen); başlangıç ekranına 0-400 arası bir sayı input'u eklendi (varsayılan 20).
    - Tarayıcıda 60 bot ile uçtan uca doğrulandı: herkes küçük yuvarlak bir parça olarak doğuyor, bitişik toprağa tıklayınca birkaç saniyede geniş bir alan ele geçiriliyor, harita üzerinde önceden hiçbir sınır/isim yokken sahiplik değiştikçe sınırlar organik olarak beliriyor, her oyuncu/bot'un üzerinde güncel asker sayısı ("2.5k" gibi) görünüyor, 60 bot eş zamanlı hatasız genişliyor (sunucu/tarayıcı konsolunda hata yok).
15. **Sabit alan → süregelen cephe modeli** (2026-07-30, mevcut durum, 14. maddenin hemen ardından aynı gün) — kullanıcı geri bildirimi: "herkes sıfır gözüküyor büyükler ama 0lar" (asker sayıları görünürde hep 0), "piksel piksel tıklamak yerine tıkladığım yere doğru büyüyecek", "başkasının bölgesine tıklayınca ona doğru gidecek", "herkesin sınırı koyu bir renkle belirli olsun", "savaşırken... o beni itiyor ben onu itiyorum gibi... kesiştiğimiz yerler kırmızı olsun". Kök neden: 14. maddedeki `CAPTURE_CHUNK_MAX_TILES`=60 sınırlı, `anchorTile`'dan tek seferlik BFS ile hesaplanan kuşatma modelinde, bot AI her karar turunda (800ms) rastgele bir sınır tile'ı seçtiği için — sınır artık binlerce tile'lık bir çevre olabildiğinden — aynı oyuncunun onlarca eş zamanlı, farklı hedeflere açılmış kuşatması birikiyordu; her biri asker harcadığından toplam drenaj rejenerasyonu (0.5/tick) fersah fersah geçip askerin sürekli ~0'da kilitlenmesine yol açıyordu. **Not:** bu proje AGPL-3.0 lisanslı OpenFrontIO'nun kaynak kodunu okumama ilkesini sürdürüyor (bkz. `docs/phases/README.md`) — bu değişiklik openfront.io'nun GitHub deposu okunmadan, sadece kullanıcının tarif ettiği davranışa göre tasarlandı.
    - `GameState.ts`: `Siege`/`captureOrder`/`anchorTile` tamamen kaldırıldı, yerine `AttackOrder` geldi — artık `(playerId, targetOwnerId)` çiftine göre **tekilleştirilmiş** (aynı hedefe karşı tek cephe; farklı hedeflere karşı ayrı ayrı eş zamanlı cephe açılabilir, ama bot/oyuncu başına hedef sayısı doğal olarak komşu sayısıyla sınırlı — dolayısıyla asker drenajı da sınırlı, "hep 0" hatası kökten çözüldü).
    - Fetih artık önceden hesaplanmış sabit bir tile listesi değil: her tick, saldırganın o anki sınır (frontier) setinden hedefin topraklarına dokunan tile'lar taze taze bulunuyor (`edgeTilesFor`) ve odak noktasına (tıklanan yer) en yakın olanından başlanarak tile başına sabit bir asker maliyetiyle (`CAPTURE_TILE_COST`, karakol varsa `DEFENSE_POST_GARRISON_BONUS` eklenir) ele geçiriliyor (`tileCaptureCost`, `pickCaptureTile`). Cephe, hedefin o sınırdaki toprağı tükenene ya da iptal edilene kadar **kendiliğinden sürüyor** — tek tıklama yeterli.
    - `GameState.computeContestedTiles()`: iki oyuncu birbirine karşı aynı anda cephe açmışsa (A→B ve B→A), aradaki cephe hattı tile'larını döndürüyor. `TickMessage`/`InitMessage`'a `contestedTiles: number[]` eklendi (eskiden hiç kullanılmayan `SiegeDTO`/`sieges` alanının yerine); `renderer.ts`'e `setContestedTiles`/`drawContestedTiles` (yarı saydam kırmızı overlay, ripple/hover ile aynı katmanlı desen) eklendi.
    - `PLAYER_COLORS`'tan kırmızı çıkarıldı (`#e74c3c` → `#e84393`) — kırmızı artık sadece cephe hattı vurgusuna ayrılmış, bir oyuncu rengiyle çakışmasın diye. `renderer.ts`'teki `BORDER_DARKEN` 0.72'den 0.5'e düşürüldü (sınırlar daha belirgin).
    - Tarayıcıda doğrulandı: tek bir tıklamayla toprak, tıklanan yöne doğru saniyeler içinde (tekrar tıklamaya gerek kalmadan) sürekli büyüdü; iki bot birbirine karşı cephe açtığında aralarındaki sınırda kırmızı bir hat belirdi ve ikisi de gerçekten asker tükenmiş (0) göründü (beklenen "karşılıklı itişme" — artık istisnai bir durum, önceki gibi herkeste süregelen bir hata değil); sağ tık iptali ve tekrar tıklamayla boost hâlâ doğru çalışıyor.
16. **Şehir/Karakol dengesi + görsel renk düzeltmeleri** (2026-07-30, mevcut durum, 15. maddenin hemen ardından aynı gün) — kullanıcı geri bildirimi: "evlerin etkisi artmalı daha çok asker üretebilmeliyiz ev ekleyince", "kalkanın etki alanı gözükmeli yuvarlak şekilde o etki alanında giren düşmanı yavaşlatmalı askerini azaltmalı", "deniz rengi #3D7BAB olmalı", "ülkeler için çok açık renkler kullanılmalı, örnek: #E99A92".
    - **Şehir**: `CITY_TROOP_BONUS` 50'den 300'e çıkarıldı — asker ekonomisi 15. maddede 60'tan 2500'e ölçeklenirken bu sabit güncellenmemişti, +50 artık payı %2'ye düşürüp etkisini görünmez kılmıştı. Ayrıca yeni `CITY_TROOP_REGEN_BONUS`=0.3 eklendi: artık her şehir `tickOnce`'daki asker rejenerasyonuna (`TROOP_REGEN_PER_TICK` + şehir sayısı×bunun) da katkı yapıyor — "ev ekleyince daha çok asker üretme" artık kapasiteye ek olarak gerçek bir üretim hızı farkı da yaratıyor.
    - **Karakol**: etkisi tek bir tile'dan, `DEFENSE_POST_RADIUS`=4 tile'lık yuvarlak bir etki alanına genişletildi. `GameState.tileCaptureCost(idx, attackerId)` artık o yarıçaptaki HER düşman karakolu için `DEFENSE_POST_GARRISON_BONUS` (=20, eskiden bütün bölgeye yayılmadığı için küçültülmüştü) ekliyor (üst üste binebilir) — bu hem "yavaşlatma" (fetih daha çok asker/tick istiyor) sağlıyor. Ayrıca yeni `DEFENSE_POST_TROOP_DRAIN`=15: etki alanındaki bir tile ele geçirildiğinde saldırganın toplam askerinden anlık ek kayıp düşülüyor — "askerini azaltmalı" isteği. `renderer.ts`'e `drawDefensePostRanges` eklendi: her karakolun etrafında yarı saydam, yuvarlak bir menzil vurgusu (ikonların altında, `present()`'te önce çiziliyor).
    - **Renkler**: `WATER_COLOR` `#3D7BAB`'a (istenen tam değer), `SHALLOW_WATER_COLOR` bunun daha açık bir tonuna güncellendi. `PLAYER_COLORS` tamamen açık/pastel bir palete değiştirildi (`#E99A92`, `#9AC1E9`, `#E9D592`, `#C79AE9`, `#92E9C7`, `#E9B892`, `#D6E9E9`, `#A0A9B8`) — koyu nötr topraktan (`LAND_COLOR`) güçlü kontrast, kırmızı yine de dahil değil (cephe hattı vurgusuyla çakışmasın diye — bkz. 15. madde).
    - Tarayıcıda doğrulandı: bir oyuncu üzerinde Şehir + Karakol inşa edilip Karakol'un etrafında istenen yuvarlak menzil çemberi görüldü; deniz #3D7BAB tonunda, oyuncu toprağı açık/pastel bir renkte (ör. şeftali/bej) render edildi.
17. **Nötr toprak rengi, piksel yumuşatma, bot isimleri, bot AI'nin "hep 0" hatasının gerçek kök nedeni** (2026-07-30, mevcut durum, 16. maddenin hemen ardından aynı gün) — kullanıcı geri bildirimi: "yeşillikler de açık renkte olsun", "piksel boyutu daha ince olsun çok kalın şuan", "her oyuncunun ismi olsun 400 oyuncu varsa 400 üne de ülke ve medeniyet ismi koyalım", "asker sayıları sürekli 0 gözüküyor sürekli saldırmasınlar güzel bir algoritması olsun mantıklı hareket etsinler".
    - `LAND_COLOR` koyu yeşilden (`#2F5233`) açık/pastel bir çim tonuna (`#A8CC8C`) değiştirildi — 16. maddedeki oyuncu renklerinin kontrast notu artık geçersiz, güncellendi.
    - `renderer.ts`'in `present()`'inde `ctx.imageSmoothingEnabled` false'tan true'ya çevrildi (+ `imageSmoothingQuality: "high"`) — offscreen terrain canvas'ı (her tile = 1 kaynak piksel) büyütülürken artık bilinear filtrelenip yumuşatılıyor, önceki keskin/kalın blok görünümü yumuşadı.
    - `civilizationNames.ts` geri getirildi (14. maddede bölge sistemiyle birlikte silinmişti, git geçmişinden aynen kurtarıldı — 117 isim). `server/index.ts`'teki `spawnBots` artık bot'lara `Bot-N` yerine karıştırılmış medeniyet isimleri veriyor; isim havuzundan (117) fazla bot istenirse (`MAX_BOT_COUNT`=400) isimler ikinci turdan itibaren " 2", " 3" gibi ayırt edici bir sonekle tekrar kullanılıyor. `renderer.ts`'teki `drawPlayerLabels` artık sadece asker sayısını değil, ismi de (üstte, asker sayısının hemen üzerinde) çiziyor.
    - **"Hep 0" hatasının gerçek kök nedeni bulundu:** 15. maddedeki `(playerId, targetOwnerId)` tekilleştirmesi asker drenajını sınırlasa da, bot AI her karar turunda (800ms) `getBotAttackTarget`'ın zaten savaştığı bir hedefi bulup **tekrar tekrar boost'lamasına** izin veriyordu — bu, birkaç saniye içinde her cepheyi `MAX_BOOST`=4'e kilitleyip (`SIEGE_DAMAGE_PER_TICK`×4=16 asker/tick) rejenerasyonun (0.5-0.8/tick) hiç yetişemeyeceği bir sürekli drenaja yol açıyordu. Düzeltme, `GameState.getBotAttackTarget`'ta üç kural: (1) zaten savaştığı bir hedefi bir daha SEÇMEZ (insan oyuncunun bilinçli tekrar tıklaması gibi boost'lamaz, her cephe boost=1'de kendi temposunda ilerler), (2) `BOT_MAX_CONCURRENT_ORDERS`=3 hedefe ulaştıysa yeni cephe açmaz, (3) asker `BOT_ATTACK_TROOP_FRACTION`=0.4'ün altındaysa hiç yeni cephe aramaz. Ayrıca `server/index.ts`'teki bot karar döngüsü, asker `BOT_REST_TROOP_FRACTION`=0.15'in altına düşerse o bot'un TÜM cephelerini `cancelAttacks` ile iptal edip **dinlenmeye** sokuyor (rejenerasyon gerçekten toplansın diye) — önceki gate sadece yeni cephe açmayı engelliyordu, zaten aktif olan cephe askerini sıfıra çekmeye devam ediyordu.
    - Tarayıcıda 150 bot ile doğrulandı: önceden ısrarla "0" gösteren bot'lar artık değişken, sağlıklı asker sayıları gösteriyor (ör. bir bot 185 askerden 198 toprağa büyürken 77 asker gösterdi) — sürekli maksimum saldırganlık yerine büyü/dinlen ritmiyle daha "mantıklı" davranıyorlar.

## Bilinen ve Düzeltilen Hatalar

- **XSS açığı** (client, panel render): oyuncu isimleri `innerHTML` ile basılıyordu, kötü niyetli bir `join` mesajı script çalıştırabilirdi. `textContent` tabanlı güvenli DOM oluşturmaya çevrildi.
- **Genişleme zinciri kırılması** (tile-flood döneminde): bir tile zaten kendine aitse komşularını taramadan zincir kesiliyordu (iki emrin aynı noktada birleşmesi gibi durumlarda genişleme erken duruyordu). Düzeltildi.
- **Kuşatma sonsuz donması** (bölge modelinde, o dönemin hatası): asker azaldığında kuşatma hasarı bölgenin pasif garrison rejenerasyon hızına (0.5/tick) tam eşitlenip **sonsuza dek kilitleniyordu** — ikisi birbirini götürüyordu. O zamanki çözüm, kuşatma altındaki bölgelerin pasif rejenerasyon almaması oldu. Not: "orantılı parçalı bölge alımı" (13. madde) ile birlikte garrison artık bölgede kalıcı değil, her kuşatmaya özel ve hiç pasif rejenere olmuyor — bu hata sınıfı yapısal olarak imkânsız hale geldi (pozitif hasar her zaman kesin olarak azalır).
- **Kamera harita dışına taşıp siyah alan gösteriyordu** (Faz 9, kullanıcı geri bildirimiyle bulundu): `clampCamera()` zoom'u hesaba katmadan kamerayı `[0,width]`/`[0,height]`'e kırpıyordu; yakınlaştırılmış haldeyken kamera harita kenarına gidince ekranın bir kısmı haritanın dışına (siyah arka plana) taşıyordu — özellikle kıyı şeridi bbox kenarına yakın bölgelerde belirginleşiyordu. Düzeltildi: kırpma artık geçerli zoom'daki görünür alanı (canvas/scale) hesaba katıp kamerayı harita kenarından en az yarım-viewport içeride tutuyor.

## Bilinçli Basitleştirmeler / Sınırlamalar

- Gerçek dünya haritaları var (Avrupa, Afrika, Kuzey Amerika, Dünya) + prosedürel "Rastgele Ada" (Faz 8+9); OpenFront'un onlarca haritasıyla sayı yarışı hedeflenmiyor, bilinçli olarak 5 ile sınırlı tutuldu. "Dünya" haritası düşük çözünürlükte (570×220, ~1.6px/derece) — küçük ada ülkeleri zar zor render olabilir, bu bilinçli bir ödünleşim (bkz. Faz 9 açık sorular).
- Tek oyun modu (herkese karşı herkes). Takım modu, özel lobi yok. Harita seçimi + bot sayısı var ama tam lobi UI'ı değil — sadece join-öncesi bir dropdown + sayı input'u (Faz 12'de gerçek lobi sistemi gelecek).
- Bölge/ülke kavramı yok (2026-07-30'da kaldırıldı) — harita tamamen açık, isimli bölgeler/sınırlar önceden çizilmiyor; sadece fiili sahiplik sınırları var. Bkz. "Geliştirme Süreci" 14. madde.
- Liman + ticaret gemisi (Faz 6) ve savaş gemisi + deniz savaşı (Faz 7) var. Kıyı savunma yapılarının gemilere ateş etmesi yok; yakalanan ticaret gemisi el değiştirmiyor, doğrudan yok oluyor (bonus altın karşılığında).
- Nükleer silahlar (atom bombası, MIRV, SAM) yok.
- Tren/demiryolu sistemi yok.
- İttifak/diplomasi sistemi yok.
- Mobil/dokunmatik kontrol yok (sadece fare).
- Hosting/deploy yapılmadı — sadece local `npm run dev` ile çalışıyor.

## Bundan Sonra Yapılmak İstenenler (öneri sırası)

> Her maddenin ayrıntılı teknik planı — veri modeli, sunucu/istemci değişiklikleri, önerilen denge sabitleri, yapılacaklar listesi, kabul kriterleri ve açık sorularıyla birlikte — [`docs/phases/`](docs/phases/README.md) altında ayrı bir dosyada. Buradaki liste sadece hızlı bir özet; uygulamaya geçmeden önce ilgili faz dosyası okunmalı.

1. ~~**Liman + Ticaret Gemisi**~~ ([faz-06](docs/phases/faz-06-liman-ticaret-gemisi.md)) — **tamamlandı.**
2. ~~**Savaş Gemisi + deniz savaşı**~~ ([faz-07](docs/phases/faz-07-savas-gemisi-deniz-savasi.md)) — **tamamlandı.**
3. ~~**Gerçek dünya haritası**~~ ([faz-08](docs/phases/faz-08-gercek-dunya-haritasi.md)) — **tamamlandı** (Avrupa).
4. ~~**Çoklu harita desteği**~~ ([faz-09](docs/phases/faz-09-coklu-harita.md)) — **tamamlandı** (Avrupa, Afrika, Kuzey Amerika, Dünya, Rastgele Ada — 5 harita).
5. **Nükleer silahlar** ([faz-10](docs/phases/faz-10-nukleer-silahlar.md)) — Atom bombası → Hidrojen bombası → MIRV, SAM savunması.
6. **İttifak/diplomasi sistemi** ([faz-11](docs/phases/faz-11-ittifak-diplomasi.md)).
7. **Takım modları, özel lobiler** ([faz-12](docs/phases/faz-12-takim-modlari-lobiler.md)).
8. **Tren/demiryolu** ([faz-13](docs/phases/faz-13-fabrika-tren.md)) (deneysel, en son — OpenFront'ta da en son eklenen özellik).
9. Gerekirse: canlıya alma/hosting ([faz-14](docs/phases/faz-14-hosting-deploy.md)) (şu an 1-2 oyuncu için hiç gerek yok, ücretsiz katmanlar fazlasıyla yeterli).
10. **Görsel iyileştirmeler** ([faz-15](docs/phases/faz-15-gorsel-iyilestirmeler.md)) — mekanik eklemiyor, mevcut Canvas2D render'ı (su/kıyı, bölge sınırları, bina/birim ikonları, HUD, kamera geçişleri, opsiyonel minimap) cilalıyor; kendi içinde 15.1-15.6 alt fazlarına bölünmüş, bağımsız olarak istenen noktada alınabilir.

## Nasıl Çalıştırılır

```bash
npm install
npm run build:maps   # resources/maps/{europe,africa,north-america,world}.json üretir (bir kere; kaynak: resources/geo-source/)
npm run dev           # client: http://localhost:5173, server: http://localhost:3000
```

`resources/maps/*.json` dosyaları zaten repoda mevcutsa `build:maps` adımı atlanabilir — sadece dosyalar yoksa (veya haritaları yeniden üretmek isterseniz) gerekir. Bir harita dosyası bulunamazsa sunucu placeholder dairesel adaya düşer (konsola uyarı basar).

Oyuna bağlanmadan önce açılan ekrandan harita (Avrupa/Afrika/Kuzey Amerika/Dünya/Rastgele Ada — bkz. `src/core/maps.ts`) ve bot sayısı (0-400, varsayılan 20) seçilir, sonra "Oyuna Katıl" ile bağlanılır. Not: oyun zaten kurulu (başka biri katılmışsa) farklı bir harita/bot sayısı seçimi yok sayılır, mevcut oyuna katılınır.

Kontroller: tekerlek=zoom, sürükle=pan, tık=kendi toprağına bitişik bir tile'a saldır (tekrar tıkla=hızlandır), sağ tık=saldırıları/gemi seçimini iptal et, alttaki bar'dan bina seç (Şehir/Karakol/Liman/Savaş Gemisi) + kendi toprağına (Savaş Gemisi için kendi limanına) tıkla=inşa et (Liman için tıklanan tile'ın kıyıya bitişik olması gerekir). Kendi savaş gemine tıkla=seç, sonra suya tıkla=hareket ettir.
